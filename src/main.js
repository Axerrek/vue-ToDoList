import { createApp } from "vue";
import App from "./App.vue";

createApp(App).mount("#app");
// Import zewnętrznego pliku CSS
import "./assets/styles.css"; // lub './styles/ListStyles.css' jeśli umieściłeś plik w innym katalogu
